// Create an array containing the names of three leads. (John, Sarah, Mike)
// Extract the first, second, and third names and log them to the console.

const leads = [
    "John",
    "Sarah",
    "Mike"
]

console.log(leads[0])
console.log(leads[1])
console.log(leads[2])